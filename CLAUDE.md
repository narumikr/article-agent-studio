# プロジェクトガイドライン

このプロジェクトはClaudeのエージェントチームを用いた技術調査の環境設定を行なったものです。

ユーザーはClaudeのCLIなどを用いてエージェントチームを作成し、必要な調査を実施してもらうことが可能となっています。

## 運用規約

以下の規則は必ず遵守してください。

- ユーザーとのやり取りは日本語で行なってください。
- エージェントチームはClaudeのコマンドから実行されたときのみ作成し、自然言語で依頼されたときはコマンドから実行することをユーザーに案内してください。コマンドは`.claude/commands`にあります。
- ドキュメント生成における細かなルールは`.claude/rules`にあるので確認してください。
- ハルシネーションを防ぐために必ず調査内容のソースを提示すると共にレビューも実施してください。

## ディレクトリ構成

```text
<root>/                         プロジェクト全体のルート
├── CLAUDE.md
├── .claude/									  ClaudeCodeのルールやコマンドファイルなど格納
└── docs/                       ドキュメント一式をまとめる
     ├── images/                図版・スクリーンショットなどの画像を格納
     ├── library/               資料・知識ベースを格納
     │   └── <sub-dir>/         テーマ別・用途別に資料を格納するフォルダ群
     └── outputs/               生成物や作業結果の一次ファイルを格納
         └── <output-file>.md   任意の出力内容ファイル
```